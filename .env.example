# Rename this file to `.env` and fill in your real values
# Never commit your real .env to source control

# Required
OPENAI_API_KEY=

# Optional: if your key is a project-scoped key, set this
# https://platform.openai.com/settings/organization/general
OPENAI_PROJECT=

# Optional: legacy org field (usually not needed with project keys)
OPENAI_ORG=
